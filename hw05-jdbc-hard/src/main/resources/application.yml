spring:
  datasource:
    url: jdbc:h2:mem:library;DB_CLOSE_DELAY=-1;INIT=create schema if not exists library\;SET SCHEMA library
  h2:
    user-name: sa
    password:
  liquibase:
    enabled: true
    url: jdbc:h2:mem:library;DB_CLOSE_DELAY=-1;INIT=create schema if not exists library\;SET SCHEMA library
    user: sa
    password:
