spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;INIT=create schema if not exists testdb\;SET SCHEMA testdb
    initialization-mode: always
  h2:
    user-name: sa
    password:
  liquibase:
    enabled: true
    contexts: test
    change-log: classpath:db-test/changelog/db.changelog-master-test.yaml
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;INIT=create schema if not exists testdb\;SET SCHEMA testdb
    user: sa
    password:
